apiVersion: apps/v1
kind: Deployment
metadata:
  name: nfs-server
  namespace: nfs-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nfs-server
  template:
    metadata:
      labels:
        app: nfs-server
    spec:
      hostNetwork: true
      nodeSelector:
        kubernetes.io/hostname: ripnix
      containers:
      - name: nfs-server
        image: erichough/nfs-server
        securityContext:
          privileged: true
        env:
        - name: NFS_VERSION
          value: "4"
        - name: NFS_EXPORT_0
          value: /exports/plex *(rw,fsid=0,async,no_subtree_check,no_auth_nlm,insecure,no_root_squash)
        volumeMounts:
        - name: plex-data
          mountPath: /exports/plex
        - name: nfs-fs
          mountPath: /proc/fs/nfsd
        - name: nfs-modules
          mountPath: /lib/modules
          readOnly: true
      volumes:
      - name: plex-data
        hostPath:
          path: /MASS/Plex
          type: Directory
      - name: nfs-fs
        hostPath:
          path: /proc/fs/nfsd
      - name: nfs-modules
        hostPath:
          path: /lib/modules
